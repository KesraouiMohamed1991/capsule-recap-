'use client';
import Image from "next/image";
import BootcampProgress from "./BootcampProgress";
export default function Home() {
  return (

      <main className="w-full h-full font-[family-name:var(--font-geist-mono)] flex gap-6 ">
<BootcampProgress/>
      </main>
  );
}
